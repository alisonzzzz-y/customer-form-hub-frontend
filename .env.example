# Backend API origin. Leave unset for local development (defaults to
# http://localhost:8080). On Vercel set this to the Render backend URL —
# and set CORS_ALLOWED_ORIGINS on the backend to the Vercel URL.
VITE_API_BASE=http://localhost:8080
